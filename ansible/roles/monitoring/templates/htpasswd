# ansible-managed in ooni/sysadmin.git
# Username should be taken from @openobservatory.org domain
# Password should be generated with scripts/ngx-mkpasswd
# don't remove federico, because it will break monitoring
{{ monitoring_htpasswd }}
